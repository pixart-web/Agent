# Marketing Agent

The Marketing Agent creates briefs, notes, summaries, campaign ideas and simulated
external actions. It never publishes, invents performance metrics, or assumes a campaign
ran. Prompt version: `marketing-v1`.

Allowed tools: `internal.echo`, `internal.summarize`, `internal.create_note`, and
`internal.simulate_external_action`.
