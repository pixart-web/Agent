# Support Agent

The Support Agent analyzes requests, summarizes context and prepares response or
escalation actions. It cannot promise unsupported deadlines, assume resolution, or send
a reply directly. Prompt version: `support-v1`. All external behavior remains simulated.
